# Negotiation Engine — runtime dependencies.
#
# Pin minimum versions only; the conda env (`infosys_project`) provides
# the actual installed versions. Pip resolution should be a no-op if
# the env is healthy.

# Web server (host conda env provided these implicitly; the container needs
# them explicitly so `uvicorn src.main:app` in the Dockerfile can start).
fastapi>=0.110
uvicorn[standard]>=0.27

# Core LangGraph + LangChain stack.
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# Pydantic v2 (per CLAUDE.md).
pydantic>=2.11
pydantic-settings>=2.0

# Async I/O.
aiohttp>=3.9
redis>=5.0

# Step 3 additions — Kafka audit + durable checkpointing.
aiokafka>=0.10
langgraph-checkpoint-postgres>=2.0
psycopg[binary,pool]>=3.2

# Step 4+ additions (Qdrant agent memory) — placeholder for next step.
# qdrant-client>=1.9

# Test.
pytest>=8.0
pytest-asyncio>=0.23
