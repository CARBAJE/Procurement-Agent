# Frontend Demo Gateway — runtime dependencies.
#
# The gateway imports production code from sibling services
# (services/ComparativeAndScoreing for the PyTorch model and
# services/negotiation_engine for the LangGraph state machine), so the
# union of their runtime needs is required here as well.

# FastAPI + Pydantic stack.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.11
pydantic-settings>=2.0

# PyTorch for the Phase-2 nn.Linear LTR model.
torch>=2.1
numpy>=1.26

# LangGraph for the Phase-3 negotiation state machine.
langgraph>=0.2
langchain-core>=0.3

# Live wiring: OpenAI SDK (points at local Ollama, NOT api.openai.com) for the
# qwen3:8b Supplier Agent; redis for the on_select resume broker.
openai>=1.40
redis>=5.0

# Tests.
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
httpx>=0.27
