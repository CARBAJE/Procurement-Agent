# Test dependencies for the ComparativeAndScoreing service.
# Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-test.txt

pytest>=8.0.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0
httpx>=0.26.0
